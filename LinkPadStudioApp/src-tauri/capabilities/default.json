{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-events",
  "description": "Permite que a janela principal acompanhe eventos de progresso emitidos pelo backend.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
